version: '3.8'

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  app:
    build: .
    depends_on:
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
      - INITIAL_USER_IDS=${INITIAL_USER_IDS:-rW89DDsxrVs4tl1yXztPhsL-r_bmTXKsjmnvgVYeCQd8spzzuwMlBnHbFJUNMrD6Wb2ReiOTCNmgkg, pWjqIfFpXyRUixJgiqFaagTC0kJEsuSSpBtGlVnlp5aNciPiRCYMNbasqZMxI5Fx1iEDSGLtD_ObTA}
    volumes:
      - .:/app
    command: python main.py

  celery:
    build: .
    depends_on:
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
    volumes:
      - .:/app
    command: celery -A celery_app worker --beat --loglevel=info

volumes:
  redis_data:

